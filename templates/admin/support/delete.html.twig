{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
            {{ form_errors(form) }}
            <button class="btn">{{ button_label|default('Delete') }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}